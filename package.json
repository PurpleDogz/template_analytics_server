{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable",
    "test": "mocha 'tests/unit/**/*.[tj]s'"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.2",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "mocha": "^10.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
